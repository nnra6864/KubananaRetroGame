# Retro Game

### Great name for a project I'm aware

## Building

Clone the project with `git clone --recursive https://github.com/Kubanana/RetroGame.git` it needs to be a recursive clone or it will not work since dependencies are vendored.

Make sure you have `CMake` installed that is the only dependency.

And then run the `build.bat` or `build.sh` respectively.
